<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.star.cloud.rbac.dao.SysUserDao">



    <!-- insert sys_user -->
    <insert id="insert">
        INSERT INTO sys_user(
        id,
        login_name,
        password,
        name,
        email,
        mobile,
        create_by,
        create_date,
        update_by,
        update_date,
        del_flag
        ) VALUES (
        #{id},
        #{loginName},
        #{password},
        #{name},
        #{email},
        #{mobile},
        #{createUser.name},
         #{createUser.createDate},
           #{updateUser.name},
         #{updateUser.updateDate},
         #{delFlag}
        )
    </insert>

    <!-- update sys_user -->
    <update id="update">
        UPDATE sys_user SET
        login_name = #{loginName},
        no = #{no},
        name = #{name},
        email = #{email},
        mobile = #{mobile}
        WHERE id = #{id}
    </update>

    <!-- update sys_user -->
    <select id="findList" resultType="com.star.cloud.rbac.entity.SysUser">
        SELECT id,
        login_name,
        password,
        name,
        email,
        mobile FROM sys_user
    </select>

</mapper>