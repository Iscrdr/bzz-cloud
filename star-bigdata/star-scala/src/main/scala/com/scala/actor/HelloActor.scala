package com.scala.actor

class HelloActor/* extends Actor */{

  /*override def act(): Unit = {
    while(true) {
      receive {
        case "start" => {
          println("starting...")
          Thread.sleep(5000)
          println("stared")
        }
        case "stop" => {
          println("stoping ...")
          Thread.sleep(5000)
          println("stopped ...")
        }
      }
    }
  }*/
}

